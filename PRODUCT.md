# Product

> 最后更新：2026-09-01

## Register

`product`

## 产品定位

信息收集工作台是一套面向已获授权信息核验人员的 ICP 备案查询工具。它以企业数据源识别根企业和关联投资主体，再查询这些企业名下的网站备案，并把来源、范围、状态、耗时和错误保存成可追踪的查询记录。

页面名称虽然是“ICP备案查询”，但企业关系采集仍是备案查询前的主体发现步骤；结果中同时保留“对外投资”和“ICP备案”两类数据。

## 用户

- 企业资产与备案核验人员。
- 安全测试或资产梳理中已获得明确授权的操作人员。
- 需要复查失败、部分成功、分页缺失和云函数路由状态的排障人员。

## 核心用户任务

1. 输入企业名称并选择企业数据源。
2. 设置投资穿透深度和最低持股比例。
3. 获取根企业及符合条件的关联企业名称。
4. 查询各企业的 ICP 备案并判断结果是否完整。
5. 在“对外投资 / ICP备案”之间切换、分页查看和导出。
6. 在历史列表中筛选记录，点击后进入单独详情视图，再返回列表。
7. 在基础配置中维护数据源登录状态和 SeaMoon 云函数。

## 当前页面

### ICP备案查询

- 横向紧凑查询表单。
- 最近查询快捷项。
- 结果头展示企业名、任务状态、查询用时和导出操作。
- 使用分段控件切换“对外投资”和“ICP备案”。
- 结果分页固定在卡片底部，表格内容区域独立滚动。

### 历史查询

- 服务端分页列表，支持企业关键字与状态筛选。
- 支持单条或批量导出、删除。
- 点击企业名后，历史列表被详情视图替换，而不是上下同时出现。
- 详情页提供“返回列表”、状态、查询用时、Excel导出和结果分页。

### 基础配置

- 使用紧凑分段控件切换“数据源配置”和“云函数配置”。
- 数据源页维护爱企查、快查、风鸟扫码登录状态。
- 云函数页支持阿里云 FC、腾讯云 SCF 一键部署、测试、停止和删除。

## 结果口径

- “ICP备案总数”是本次任务中所有已查询企业的合计，不等于根企业单独数量。
- 每条 ICP 结果展示公司、主体备案号、服务备案号、域名、主办单位性质和更新时间。
- 分页出现重叠时，按“域名 + 服务备案号”去重。
- 未达到工信部接口报告总数时，企业查询不得静默标记成功。
- 某些企业返回0条表示接口按当前企业全称没有返回网站备案，不代表曾用名或其他运营主体下必然没有备案。

## 产品原则

1. **完整性优先。** 数量不一致、超时和部分结果必须明确显示。
2. **根企业优先。** 用户输入的企业先于关联企业执行 ICP 查询。
3. **单一任务面。** 历史列表与记录详情互斥显示，避免页面同时出现两套表格和分页。
4. **口径明确。** 根企业数量、关联企业数量、任务合计和当前分页范围不能混淆。
5. **操作可追踪。** 保存查询时间、状态、错误、来源和原始响应。
6. **熟悉胜于新奇。** 使用稳定的表格、分页、标签、表单和返回操作。
7. **代理边界清晰。** 企业数据源始终直连，SeaMoon 仅服务于 ICP 阶段。

## 状态语义

- `queued`：等待 worker 领取。
- `running`：正在查询。
- `succeeded`：全部阶段完成且没有记录错误。
- `partial`：已保存部分有效结果，但至少一个数据源或企业查询失败。
- `failed`：任务没有得到可用结果。
- `cancelled`：任务已取消。

## 非目标

- 不提供公共代理池或“查询代理”模块。
- 不保证云函数固定出口IP。
- 不提供账号体系、租户隔离或公网 SaaS 安全边界。
- 不支持分支机构采集。
- 不做破坏性验证、持久化攻击或越权扩散。
- 不把原始 JSON 直接铺在主页面。

## 品牌性格

专业、克制、准确。界面应像可靠的作业台账：数据和状态是主角，视觉元素只服务于扫描、核对和排障。

## 无障碍

以 WCAG 2.2 AA 为基线。核心操作支持键盘和可见焦点；状态同时使用文字和图标；正文与控件保持可读对比度；紧凑布局不能以牺牲点击目标为代价。
