# Product

> 最后更新：2026-09-09

## Register

`product`

## 产品定位

信息收集工作台是一套面向已获授权信息核验人员的作业台账。它先用企业数据源识别根企业和符合条件的对外投资主体，再按企业全称查询工信部网站备案，并可对备案主域名做子域名发现。查询过程、结果、状态、耗时和错误都会保存成可追踪的历史记录。

页面名称是“ICP备案查询”，但企业关系采集仍是备案查询前的主体发现步骤；结果中同时保留“对外投资”和“ICP备案”两类数据。子域名查询是独立任务，也可以从已完成的 ICP 结果选取主域名继续发现。

## 用户

- 企业资产与备案核验人员。
- 安全测试或资产梳理中已获得明确授权的操作人员。
- 需要复查失败、部分成功、分页缺失、代理路由和数据源状态的排障人员。

## 核心用户任务

1. 输入企业名称，选择企业数据源、投资穿透深度和最低持股比例。
2. 在查询页选择本轮代理：云函数、自定义 HTTP 代理或直连。
3. 获取根企业及符合持股条件的关联企业，并判断 ICP 备案是否完整。
4. 在“对外投资 / ICP备案”之间切换、分页查看、停止进行中的任务并导出 Excel。
5. 对已获授权的单个、批量或 ICP 备案主域名执行子域名发现，实时核对 DNS / HTTP 结果。
6. 在历史列表中按类型、关键词和状态筛选 ICP 与子域名记录，点击后在当前页查看详情再返回列表。
7. 在基础配置中维护数据源登录、可选 FOFA / Hunter API Key、云函数凭证和自定义代理，不在配置页选择本次查询路由。

## 当前页面

### ICP备案查询

- 横向紧凑查询表单：企业名称、数据源、深度、最低持股比例、代理。
- 提交后立即创建后台任务并进入详情，不阻塞浏览器请求。
- 最近查询快捷项。
- 结果头展示企业名、任务状态、查询用时、ICP 缓存/实时企业数、停止和导出。
- 投资关系、ICP 结果和进度通过 SSE 实时增加；刷新后先载入当前快照，再从游标继续接收。实时流由已提交的数据变更唤醒，不依赖固定高频轮询。
- 分段控件切换“对外投资”和“ICP备案”。
- 结果分页固定在卡片底部，表格内容区域独立滚动；大结果集使用虚拟滚动。空白查询页不加载结果表运行时代码，开始查询或打开历史结果时提前加载。
- 导出一个 Excel 兼容文件，包含“对外投资”和“ICP备案”两张表。

### 子域名查询

- 支持手动输入单个/批量主域名，或多个 ICP 历史记录中勾选备案域名。
- 从 ICP 结果发起的任务名形如“中国烟草总公司 180 个主域名”，而不是第一个域名。
- 可开关被动源、DNS 字典、智能变体和 HTTP 探测；被动源与字典至少启用一项。
- 公开被动源默认参与；FOFA / Hunter 仅在基础配置填写 API Key 后才会请求。
- DNS 先验证并实时出现，HTTP 状态、标题和访问地址随后补全；响应正文只提取同主域名引用，不持久化正文。
- 展示主域名、子域名、解析地址、泛解析、HTTP 状态、标题、来源和发现时间。
- 支持搜索、全部 / 网站可访问 / 泛解析筛选；任务完成后由服务端执行过滤并返回分类总数，不只筛选浏览器当前页。
- 打开任务先加载一页已保存结果；运行中靠 SSE 增量；完成后按页向服务端取当前页。SSE 由已提交的数据变更唤醒，自动重连从最后确认的事件游标恢复，导出 Excel 时再拉全量。
- 最近查询可切换；进行中的任务可停止，已发现结果保留。

### 历史查询

- 统一列出 ICP 备案查询和子域名查询。
- 服务端分页，支持关键词、状态和类型筛选。
- 支持单条或批量导出、删除。
- 点击记录后，列表被详情视图替换，而不是上下同时出现两套表格。
- 详情提供“返回列表”、状态、查询用时和 Excel 导出。
- 子域名详情按页加载结果，导出再拉全量。

### 基础配置

- 紧凑分段控件切换“数据源配置”和“代理池配置”。
- 数据源页：爱企查、快查、风鸟扫码登录；可选填写 FOFA 邮箱 / API Key 和 Hunter API Key。密钥已保存时前端只显示“已保存”，不会回传明文。
- 代理池页：云函数凭证（云平台、AccessKey ID、AccessKey Secret）和自定义 HTTP 代理列表。地域和函数名由平台按云厂商写入默认值，页面不展示节点池。
- 本轮查询走哪条路由，在 ICP 备案查询页选择，不在基础配置里选择。

## 代理与数据源边界

- ICP、爱企查、天眼查跟随查询页代理选择；同一轮不混用云函数、自定义代理和直连。
- 快查、风鸟和所有扫码登录链路始终直连。
- 选择云函数但没有就绪节点，或选择自定义代理但没有可用入口时，不得静默改走直连。
- 云函数按任务规模自动扩容，任务结束后只回收平台自动创建的闲置节点。
- 自定义代理按入口轮询；快代理这类入口一次请求对应一个出口 IP。
- FOFA / Hunter 只用于子域名候选发现，不替代 DNS 验证，也不参与 ICP 查询。

## 结果口径

- “ICP备案总数”是本次任务中所有已查询企业的合计，不等于根企业单独数量。
- 每条 ICP 结果展示公司、主体备案号、服务备案号、域名、主办单位性质和更新时间。
- 分页出现重叠时，按“域名 + 服务备案号”去重。
- 未达到工信部接口报告总数或缺少 `total` 时，企业查询不得标记成功；已获取记录仍保留并可导出，不写入完整缓存。
- 投资分页按主体标识去重并核对总数；缺页保留部分关系，继续遍历已知子企业。共享企业按最浅深度处理，避免并发顺序导致漏查。
- 子域名已发现候选不按数量截断；来源尚有未读取页或 DNS 持续暂时失败时，明确显示“结果不完整”，已有解析结果仍保留，任务为部分成功。
- 只复用未过期、算法版本一致且唯一记录数等于工信部报告总数的企业级缓存；不完整、失败和旧版本结果必须实时重查。
- 某些企业返回 0 条表示接口按当前企业全称没有返回网站备案，不代表曾用名或其他运营主体下必然没有备案。
- 子域名“已发现”是通过 DNS 验证后的去重主机数；“网站可访问”是已探测到 HTTP 状态码的子集。没有状态码不代表域名无效。
- 子域名可选被动源局部失败记为提示；已发现可解析结果且没有确认的完整性缺口时可记为成功。确认的分页或解析缺口始终标记部分成功。

## 产品原则

1. **完整性优先。** 数量不一致、超时和部分结果必须明确显示。
2. **根企业优先。** 用户输入的企业先于关联企业执行 ICP 查询。
3. **边发现边查询。** 投资关系遍历过程中符合条件的企业立即进入 ICP，不必等整棵树结束。
4. **单一任务面。** 历史列表与记录详情互斥显示，避免页面同时出现两套表格和分页。
5. **口径明确。** 根企业数量、关联企业数量、任务合计、已加载条数和当前分页范围不能混淆。
6. **操作可追踪。** 保存查询时间、状态、错误、来源和原始响应。
7. **熟悉胜于新奇。** 使用稳定的表格、分页、标签、表单和返回操作。
8. **代理边界清晰。** 查询页选择路由；基础配置只保存凭证和代理列表。失败后不得静默直连。
9. **密钥按需启用。** FOFA / Hunter 未配置时跳过，不把需要积分的情报源隐式打开。

## 状态语义

- `queued`：等待 worker 领取。
- `running`：正在查询。
- `succeeded`：全部阶段完成且没有确认的完整性缺口；子域名可保留普通可选来源提示。
- `partial`：已保存部分有效结果，但至少一个数据源或企业查询失败。
- `failed`：任务没有得到可用结果。
- `cancelled`：任务已取消，已写入结果保留。

## 非目标

- 不提供公共免费代理池或已删除的“查询代理”模块。
- 不保证云函数固定或互不相同的出口 IP。
- 不提供账号体系、租户隔离或公网 SaaS 安全边界。
- 不支持分支机构采集。
- 不默认开启接管检查、端口扫描、AXFR 或其它破坏性验证。
- 不做持久化攻击或越权扩散。
- 不把原始 JSON 直接铺在主页面。
- 不把 FOFA / Hunter 的原始资产检索结果未经 DNS 验证直接当作有效子域名。

## 品牌性格

专业、克制、准确。界面应像可靠的作业台账：数据和状态是主角，视觉元素只服务于扫描、核对和排障。

## 无障碍

以 WCAG 2.2 AA 为基线。核心操作支持键盘和可见焦点；状态同时使用文字和图标；正文与控件保持可读对比度；紧凑布局不能以牺牲点击目标为代价。
